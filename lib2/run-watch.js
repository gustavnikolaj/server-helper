/**
 * Run a server in watch mode.
 *
 * @param {{
 *  entrypoint: string,
 *  port: number,
 *  host?: string
 * }} options
 */
function runWatch(options) {
  console.log("Running watch mode with options", options);

  throw new Error("Watch mode NYI");
}

module.exports = runWatch;
